The general idea was to utilize only the command line, but gitbash opened up a few popups. I use Git to manage local repositories on a server without a graphical environment, so I wonder if the process will be any different there.
